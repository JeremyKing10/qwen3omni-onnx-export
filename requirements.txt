# Qwen3-Omni ONNX 导出工具依赖
# Python 3.11；创建虚拟环境后安装。
# transformers 不在此安装：必须用固定源码（见 scripts/bootstrap.sh / README）。

# 深度学习框架（Linux + CUDA 时请按 pytorch.org 指令选择对应 wheel）
torch==2.8.0

# Hugging Face 生态
accelerate==1.15.0
safetensors==0.8.0
huggingface_hub>=0.34

# ONNX 工具链（已验证组合）
onnx==1.22.0
onnxruntime==1.30.0
onnxscript==0.7.2

# 其他
numpy>=2.0
psutil>=5.9
