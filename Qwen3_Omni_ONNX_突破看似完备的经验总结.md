# 如何突破“看似正确完备”的幻觉

> 本文不是验收报告，而是**方法论复盘**：为什么一个“检查都跑通、结论都正确”的版本仍会暴露大量问题；以及在当前版本同样“看上去完好”的前提下，怎样系统地发掘新问题。
>
> 案例全部来自本项目真实过程：`616c5a4`（声称完备）→ `ab2b07e`（又新增约 +2795/−890 行代码与 970 行测试）。

---

## 0. 核心结论（先看这五句）

1. **“两套实现结果一致”不等于“其中任何一套正确”。** 它们可能共享同一个错误。
2. **“我检查过”不等于“已实测”。** 静态推断写成事实，是本项目最大的失真来源。
3. **修复的强度必须不低于验收的强度。** 否则修复本身就是新的风险来源。
4. **没有常驻测试，结论不可复现，换上下文即丢失。** 叙述无法替代回归。
5. **完备不是状态，而是“已验证范围 + 已知未验证项”的清单。**

---

## 1. 本项目实证：一个“完备版本”里藏着什么

`616c5a4` 当时的判断是：修复完成、命令全跑通、数值全绿。随后发现：

| 类别 | 当时结论 | 实测/复查结果 |
|---|---|---|
| 多模态语义 | 端到端“已验证” | 图像特征被填到位置 1–4、音频 5–7，而 token 占位是 2–5、8–10；**PT 与 ONNX 使用同一错误布局，比较仍通过**；改用对齐布局后 logits 差 **0.2558** |
| 参考独立性 | “与官方 forward 等价” | 参考实现基本复制 Wrapper 的注入与缓存逻辑，无法发现 Wrapper 的语义错误 |
| 精度 | “CPU 不能执行 BF16” | 仅普通 NumPy 喂数失败；改用显式类型 IOBinding 后，CPU 上 BF16 Cast/Identity **可执行** |
| 位置计算 | 位置已统一 | `int(rope_delta)` 截断合法分数位置；官方视频 `delta=0.30000019`，正确下一位置 **6.3** |
| 产物安全 | “预检后删除” | 预检即 `rmtree`；第二组件失败时第一组件已被删除 |
| 证据绑定 | “哈希链完整” | 新权重＋刷新 inspect＋旧数值报告可被接受；组件 `case` 不一致也可 `validated=True` |
| 报告可信度 | 报告即结论 | 宽容差下 999 与 1 判为通过；报告路径可能与 external 权重重名而被覆盖 |

**关键模式**：这些都不是“看代码没看清”，而是**验证结构本身允许它们通过**。

---

## 2. 六类“完备幻觉”与识别信号

| 幻觉 | 典型措辞 | 识别信号 | 破解动作 |
|---|---|---|---|
| 一致即正确 | “PT 与 ONNX 完全一致” | 两侧共用同一份宿主逻辑/同一个 helper | 追问：参考是否独立？故意改坏被测逻辑，测试会失败吗？ |
| 推断即事实 | “显然”“因此”“必然” | 结论没有对应命令/退出码/产物 | 标为“静态推断”，补实测或改写措辞 |
| 修复完成 | “全部已修好” | 修复后没有新增回归 | 每个修复配一条反向用例 |
| 汇报即状态 | “已完成”“已落盘” | 未看 `git status`/文件内容 | 以磁盘状态与测试结果为准 |
| 范围膨胀 | “数学等价”“无需改代码”“唯一阻塞” | 从有限样例推广到全规模/全 dtype | 改为“在 X 配置、Y 输入、Z 环境下通过” |
| 覆盖错觉 | “测试很全” | 新模块无测试（当前：4 个模块零测试） | 列出“模块 ↔ 测试”矩阵 |

---

## 3. 发掘新问题的可执行方法（按投入产出排序）

### 3.1 反耦合审计（收益最高）

对每一条“通过”的结论，画一条链：

```
参考/期望值 来自哪里？ → 是否经过被测代码的同一个函数？ → 若该函数错，结论会不会变？
```

本项目应逐条检查：模态注入、位置计算、掩码构造、缓存组装、特征提取。

**判据**：若期望值由被测实现自身生成，这条“通过”没有证据价值。

### 3.2 故障注入 / 变异测试

故意改坏实现，断言测试**必须失败**：

- 把模态特征整体左移一位；
- 打乱 DeepStack 行序；
- 把 `sum` 改回官方顺序之外的累加；
- 截断/取整 `rope_delta`；
- 替换 external 权重字节；
- 只刷新 inspect 不刷新 validation；
- 把报告名改成 external 权重的 location。

**判据**：改坏后仍然全绿的测试，是装饰品。

### 3.3 边界与极端输入

已在本项目产生实际收益的边界：

| 维度 | 边界 |
|---|---|
| 数值类型 | BF16 标量/空张量/共享文件、int32 与 int64、bool 掩码 |
| 序列 | `past=0`、声明上限、远超上限 |
| 文件 | 缺文件、offset 无 length、区间不足、子图 external |
| 路径 | 祖先 symlink、断链、父目录跳转、报告与权重重名 |
| 状态 | 旧报告、旧 schema、换权重、换源码、搬移目录 |
| 参数 | 容差 inf/NaN/负数、provider 不存在、`--case` 传错 |

### 3.4 三态标注

每个结论强制标注：**已实测 / 静态推断 / 未验证**。

- “已实测”必须能给出命令与退出码；
- “静态推断”不得写成保证；
- “未验证”进风险清单。

### 3.5 以状态为准，不以叙述为准

- 看 `git status`、`git diff --stat`、文件实际内容；
- 重跑命令并记录真实退出码，不用 `grep/tail` 掩盖失败；
- 数字自己重算，不复述旧报告（本项目旧 461/41 与重导出后 467/41 就是教训）。

### 3.6 换上下文复核

同一份代码换一个实现者/审查者/角色重新审视，往往能发现“自洽盲区”。本项目第二轮审查即发现了第一轮未发现的问题（耦合、预检删除、BF16 一刀切）。

### 3.7 维护“模块 ↔ 测试”矩阵

当前矩阵（真实状态）：

| 模块 | 测试 |
|---|---|
| `onnx_artifact_utils.py` | ✅ `test_artifact_utils.py` |
| `qwen3_omni_thinking_components.py` / `validate_thinking_pipeline.py` | ✅ `test_semantics.py` |
| `build_thinking_package.py` / `aggregate_operators.py` | ✅ `test_package_evidence.py` |
| `validate_onnx.py` / `inspect_onnx.py` | ✅（经 `test_artifact_utils.py` 调用） |
| `export_onnx.py` / `export_thinking_onnx.py` | ❌ **无** |
| `run_local_thinking_pipeline.py` / `run_real_thinking_pipeline.py` | ❌ **无** |

**空白格就是下一个“看似完备”的入口。**

---

## 4. “破除幻觉”检查单（每轮改动后执行）

1. [ ] 本次每条“通过”结论，参考是否与被测共享逻辑？
2. [ ] 新增/修改的代码，是否新增了反向用例？
3. [ ] 改坏实现后，相关测试会失败吗（变异）？
4. [ ] 边界清单（类型/序列/文件/路径/状态/参数）是否覆盖本次改动？
5. [ ] 结论是否标注“已实测 / 静态推断 / 未验证”？
6. [ ] 数字是否为本次重算，而非沿用旧报告？
7. [ ] 退出码是否真实记录，是否用管道掩盖失败？
8. [ ] “模块 ↔ 测试”矩阵是否出现新的空白格？
9. [ ] 文档是否有“已知未验证项”清单并同步？
10. [ ] 换一个上下文（他人/他角色/隔一段时间）复核过吗？

---

## 5. 当前版本的“已验证 / 未验证”边界（诚实账本）

**已实测（本机 tiny，48 GiB）**

- 59 项持久 unittest 全绿；
- 一键 tiny 全流程退出码 0，manifest `passed=true`、`status=tiny-interface-validation-only`；
- 467 节点 / 41 种标准算子 / 0 自定义 domain；
- 端到端 3 步 Decode，误差 vision 5.59e-09、audio 1.16e-09、prefill 8.34e-07、decode 8.34e-07；
- 事务回滚、证据失效化、并发锁（一次性探测验证正确）；
- 只读预检失败路径清晰。

**未验证（不得表述为已完成）**

- 官方 30B 权重导出与验收；
- FP16/BF16 全图在目标 provider 的内核兼容性；
- 导出/运行器四个模块的行为（无常驻测试）；
- real 代码路径（仅小配置模拟）；
- 算子/Shape/dtype 契约在真实配置下的取值；
- 源码 bytes 归档（当前仅 hash/版本/git）；
- 外部 checkpoint 目录的读取（静态推断）。

---

## 6. 一句话行动准则

**不要问“是不是都通过了”，要问“如果我故意把某处改错，会有哪条测试失败；如果没有，我凭什么说它是正确的”。**

把这个问题对每一条结论问一遍，“看似完备”的幻觉就会自然破裂。

---

## 7. 交接给其他机器 / 其他 AI Agent 的用法

这份文档**可以作为防幻觉约束交接**，但它只是约束，不是自动执行的机制。要真正起作用，接管方必须按下面五条执行。

### 7.1 必读时机

- 接手任务后、写第一行代码前，先读第 0、1、2 节；
- 每次准备说“完成/已验证/全部通过”之前，读第 2、4 节；
- 每次修改代码之后，读第 3、4 节并执行检查单；
- 代码审查（无论自己还是他人）时，读第 3.1、3.2 节。

### 7.2 输出契约（接管方每次汇报必须包含）

1. **三态标注**：每条结论标注 `已实测 / 静态推断 / 未验证`。
2. **命令与退出码**：声称已实测的，必须给出命令与真实退出码，禁止用 `grep/tail` 掩盖失败。
3. **本次重算的数字**：不得沿用旧报告中的节点数、哈希、误差。
4. **已知未验证项**：本次改动涉及但未验证的内容，逐条列出。
5. **模块 ↔ 测试矩阵变化**：新增/修改的模块是否有测试，没有就明说。

### 7.3 可被机器校验的锚点

接管方至少执行并贴出结果：

```bash
source .venv/bin/activate
python -B -m unittest discover -s tests -v      # 持久回归
python run_local_thinking_pipeline.py --offline # tiny 全流程，记录退出码
git status --short && git diff --stat           # 以磁盘状态为准，不以汇报为准
```

数值以 `manifest.json`、`operators/summary.json`、`validation/end_to_end.json` 当前内容为准重新读取。

### 7.4 交接方禁止事项

- 禁止把“两侧一致”说成“正确”；
- 禁止把静态推断写成保证；
- 禁止用放宽容差、跳过 Shape Inference、绕过身份绑定让流程“变绿”；
- 禁止复用旧 schema 或旧报告充当本次证据；
- 禁止在没有新增回归的情况下宣布修复完成；
- 禁止删除未知的事务残留目录或旧证据以“清理现场”。

### 7.5 反幻觉自问清单（每次汇报前逐条自问）

1. 我的期望值是不是被测代码自己生成的？
2. 如果我把某个关键函数改错，现有测试会失败吗？我验证过吗？
3. 我说“已实测”的每一条，命令和退出码在哪里？
4. 有没有把 tiny 结论推广到官方规模/其它 dtype/其它 provider？
5. 本次修复引入的新行为，有没有反向用例？
6. 我引用的数字，是这次重算的还是旧报告的？
7. 我有没有用 `grep/tail` 之类的管道掩盖失败？
8. 哪些内容其实没验证，但我在措辞里暗示它通过了？

---

## 8. 维护规则与更新日志

### 8.1 维护规则

- **每次代码修改后**，回来追加一条日志：改了什么、暴露了什么盲区、补了什么回归、还剩什么未验证。
- **每发现一类新的幻觉**（新的“看似正确”模式），补充到第 2 节表格。
- **每新增一个有效发掘手段**，补充到第 3 节。
- **每完成一轮验收**，更新第 5 节的“已验证 / 未验证”账本。
- 只追加与修正，不删除历史教训——被删掉的教训会重复发生。

### 8.2 更新日志

| 日期 | 提交/阶段 | 追加内容 |
|---|---|---|
| 2026-09-23 | `616c5a4` → `ab2b07e` | 创建本文。记录六类完备幻觉、七种发掘方法、十项检查单，以及当前“已验证/未验证”账本 |
| 2026-09-23 | 同上 | 实证案例：模态错位（PT/ORT 同错仍通过，对齐后 logits 差 0.2558）、参考复制 Wrapper、BF16 结论被 IOBinding 推翻、`int(rope_delta)` 截断 0.3、预检即删除、旧报告可复用、宽容差下 999 vs 1 通过 |
| 2026-09-23 | 同上 | 新增第 7 节（交接用法与输出契约）、第 8 节（维护规则与日志） |
| 2026-09-23 | 复查 | 记录当前 4 个模块无测试（`export_onnx.py`、`export_thinking_onnx.py`、`run_local_thinking_pipeline.py`、`run_real_thinking_pipeline.py`）；事务/回滚/并发锁仅经一次性探测验证，无常驻回归 |
